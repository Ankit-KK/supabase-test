
project_id = "vsevsjvtrshgeiudrnth"

[functions]
[functions.create-payment-order]
verify_jwt = false

[functions.verify-payment]
verify_jwt = false

# Create streamer_contracts table using SQL:
# CREATE TABLE IF NOT EXISTS public.streamer_contracts (
#   id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
#   streamer_type TEXT NOT NULL UNIQUE,
#   streamer_name TEXT NOT NULL,
#   signature TEXT NOT NULL,
#   agreed_to_terms BOOLEAN NOT NULL DEFAULT true,
#   signed_at TIMESTAMP WITH TIME ZONE DEFAULT now(),
#   streamer_cut INTEGER,
#   hyperchat_cut INTEGER,
#   CONSTRAINT streamer_type_check CHECK (streamer_type IN ('ankit', 'harish', 'mackle', 'rakazone'))
# );

